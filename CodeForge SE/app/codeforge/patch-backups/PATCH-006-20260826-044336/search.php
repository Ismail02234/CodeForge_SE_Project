<?php
require_once __DIR__.'/core/bootstrap.php';$user=require_login($pdo);$q=trim((string)($_GET['q']??''));$users=[];$problems=[];$universities=[];if($q!==''){$like='%'.$q.'%';$s=$pdo->prepare("SELECT id,username,rating,university,`rank` FROM users WHERE username LIKE :q OR university LIKE :q ORDER BY rating DESC LIMIT 20");$s->execute(['q'=>$like]);$users=$s->fetchAll();$s=$pdo->prepare("SELECT id,title,topic,difficulty FROM problems WHERE title LIKE :q OR topic LIKE :q OR tags LIKE :q ORDER BY title LIMIT 20");$s->execute(['q'=>$like]);$problems=$s->fetchAll();$s=$pdo->prepare("SELECT name,city FROM universities WHERE name LIKE :q OR city LIKE :q ORDER BY name LIMIT 20");$s->execute(['q'=>$like]);$universities=$s->fetchAll();}$pageTitle='Search';include __DIR__.'/includes/header.php';?>
<div class="page-head"><div><div class="eyebrow">Global Search</div><h1>Search results</h1><p><?= $q!==''?'Results for “'.e($q).'”':'Search CodeForge from the top bar.'?></p></div></div>
<div class="grid grid-3"><div class="card card-pad"><div class="card-head"><h3>Users</h3><span class="pill pill-neutral"><?=count($users)?></span></div><div class="list"><?php foreach($users as $x):?><a class="list-item" href="profile.php?id=<?=e($x['id'])?>"><div><strong><?=e($x['username'])?></strong><br><small><?=e($x['university'])?></small></div><span class="text-cyan"><?=(int)$x['rating']?></span></a><?php endforeach;?><?php if(!$users):?><span class="muted">No matches.</span><?php endif;?></div></div><div class="card card-pad"><div class="card-head"><h3>Problems</h3><span class="pill pill-neutral"><?=count($problems)?></span></div><div class="list"><?php foreach($problems as $x):?><a class="list-item" href="solve.php?id=<?=e($x['id'])?>"><div><strong><?=e($x['title'])?></strong><br><small><?=e($x['topic'])?></small></div><span class="pill <?=difficulty_class($x['difficulty'])?>"><?=e($x['difficulty'])?></span></a><?php endforeach;?><?php if(!$problems):?><span class="muted">No matches.</span><?php endif;?></div></div><div class="card card-pad"><div class="card-head"><h3>Universities</h3><span class="pill pill-neutral"><?=count($universities)?></span></div><div class="list"><?php foreach($universities as $x):?><div class="list-item"><div><strong><?=e($x['name'])?></strong><br><small><?=e($x['city']??'')?></small></div><i class="bi bi-mortarboard text-cyan"></i></div><?php endforeach;?><?php if(!$universities):?><span class="muted">No matches.</span><?php endif;?></div></div></div>
<?php include __DIR__.'/includes/footer.php';?>
